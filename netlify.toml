# Static site — no build step. Netlify serves this folder as-is.
[build]
  publish = "."
  command = ""

# Long-lived caching for fingerprint-stable assets (fonts, images, favicon).
[[headers]]
  for = "/assets/*"
  [headers.values]
    Cache-Control = "public, max-age=31536000"
